# CHANGELOG

## 2026-08-01

- 專案授權改為 GPL-3.0-or-later，新增 `LICENSE` 並同步套件 metadata。
- 清理 `custom_replacements.tsv` 中非必要的說明註解，讓字典檔只保留實際替換詞條。
- 保留 `爲 -> 為` 詞條修正。

## 2026-08-01

- 移除過度泛化的 `出 -> 齣` 自訂替換，避免「出版社」、「出版公司」、「出版日期」被誤轉為「齣版...」；保留 `一出 -> 一齣` 等具上下文詞條，並新增回歸測試。

## 2026-07-28

- 建立獨立的 `s2tw-converter` Python 工具。
- 新增 `OpenCC("s2tw")` 轉換、自訂替換前後套用、直角引號轉換。
- 新增文字、單檔與資料夾 CLI。
- 新增基本回歸測試與專案慣例文件。
- 修正輸出資料夾位於輸入資料夾內時的遞迴轉換問題。
- 輸出檔名改為自動加上 `_zyTw`。
- OpenCC 載入或轉換失敗時不再靜默略過例外。
- 整理並匯入 `epub223` 與 `metaFinder` 的自訂簡轉正詞表；兩份來源內容一致，共 302 筆，並保留 `zhTranslate` 既有本地詞 1 筆，合計 303 筆。
- 補充 `zhTranslate` 在書庫整理流程中的建議位置：作為 metadata、EPUB 可讀文字與 TXT 轉換的共用文字轉換層。
- 移除過度泛化的 `齣 -> 出` 詞條，避免覆蓋 `一出 -> 一齣` 這類具語境的臺灣正體用字。
- 從 `D:\Downloads\kiwqtshoh.txt` 保守匯入 149 筆目前不存在且非 identity 的 OpenCC 後修正詞條。
- 新增 `kiwqtshoh` 匯入詞條的回歸測試。
